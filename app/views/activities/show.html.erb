<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @activity, partial: "card", as: :activity %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "activities/activity_comments" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "activities/activity_likes" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "activities/activity_planner" %>
  </div>
</div>
  <!-- Show Page Customization end -->
